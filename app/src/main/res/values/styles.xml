<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="margins">
        <item name="android:layout_height">@dimen/edit_text_height</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginTop">@dimen/edit_text_margin_top</item>
        <item name="android:layout_marginLeft">@dimen/symmetry_margin</item>
        <item name="android:layout_marginRight">@dimen/symmetry_margin</item>
    </style>

    <style name="bntSizes" parent="margins">
        <item name="android:paddingRight">@dimen/edit_text_padding</item>
        <item name="android:paddingLeft">@dimen/edit_text_padding</item>
        <item name="android:paddingEnd">@dimen/edit_text_padding</item>
        <item name="android:paddingStart">@dimen/edit_text_padding</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="grayEditText">
        <item name="android:background">@drawable/gray_edit_text_25</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">15sp</item>
    </style>
    <style name="blueCapsButton" parent="bntSizes">
        <item name="android:background">@drawable/green_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">true</item>
    </style>
</resources>
